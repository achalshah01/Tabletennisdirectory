<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">

    <link rel="stylesheet" href="/assets/css/style.css">
    <title>Table Tennis Directory</title>
</head>
<body>
  <% if (signLabel == "Sign Out"){ %>
    <% include partials/header-signedin.ejs %>
    <% }else{ %>

  <% include partials/header.ejs %>
  <%} %>

    <section>
      <h5 id="textHide">Text</h5>
      <% if (signLabel == "Sign Out"){ %>
        <% include partials/user-navigation-signedid.ejs %>
          <% }else{ %>
      <% include partials/user-navigation.ejs %>
  <%} %>
    <% include partials/site-navigation.ejs %>

    <div class="description">
        <%if (item.length=== 0){ %>
        <article class="productArticle">
        <p>Not a valid Item according to data</p>
        </article>
        <% }else{ %>
        <article class="productArticle">
            <p class="smallFont">Home > Categories> <%= item[0].subCategory %> > <%= item[0].brand %> </p>
            <h2>
                Product details
            </h2>
            <h5 id="whitespace">Text</h5>
            <form method="POST">
            <table>
                <tr>
                    <td><img src=" <%= item[0].imageUrl %> " alt="Smiley face"></td>
                    <td class="tabelText"><h2><%= item[0].itemName %></h2>
                        <h3> <%= item[0].subCategory %> </h3>
                        <h3> Rating <%= item[0].rating %> </h3>
                    </td>
                </tr>



                <tr>

                    <td>
                            <input type="submit" value="Back" formaction="/mysaveditems">
                    </td>
                    <td>
                        <form method="POST">
                        <select name="selectRating">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option value="3">3</option>
                            <option value="4">4</option>
                            <option value="5">5</option>
                        </select>
                            <select name = "usedIt">
                                <option value="Yes">Yes</option>
                                <option value="No">No</option>
                            </select>
                            <input type="submit" value="Save Rating" formaction="/mysaveditems?action=update&user_id=<%- item[0].userId %>&item_code=
											<%= item[0].itemCode %>" >
                        </form>
                    </td>
                </tr>

                    <td>


                        <!--<input type="submit" value="Save Rating" formaction="/mysaveditems?action=update&item_code=-->
											<!--<%= item.itemCode %>" >-->

                                        </td>
                                        <td>



                    </td>


                </tr>

            </table>
            </form>
            <br>
            <div id="prodDes">
                <h4>Description</h4>
                <p> <%= item[0].Description %> </p>

                <br>
                <br>

                <br>
                <br>

            </div>

        </article>
        <% } %>
    </div>
</section>
<% include partials/footer.ejs %>

</body>
</html>
